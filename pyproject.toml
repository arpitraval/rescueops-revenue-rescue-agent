[project]
name = "rescueops"
version = "0.1.0"
description = "Slack-native revenue rescue agent with Slack AI, RTS adapter, and MCP-ready context"
requires-python = ">=3.11"
dependencies = [
    "slack_bolt>=1.28.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
